{
  "name": "@micra/event-emitter",
  "version": "0.1.0-next.3",
  "description": "Isomorphic implementation of Micra's Event Emitter contract",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/micrajs/event-emitter.git"
  },
  "author": "Olavo Amorim Santos<olavo.santos@micra.dev>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/micrajs/community/issues?q=is:issue+is:open+label:%22Project:+event-emitter%22+label:%22Type:+fix%22"
  },
  "exports": {
    ".": {
      "import": "./index.mjs",
      "require": "./index.js",
      "types": "./index.d.ts"
    }
  },
  "scripts": {
    "prepare:husky": "husky install .config/husky",
    "build": "concurrently 'pnpm:build:*'",
    "build:code": "vite build --config .viterc.ts",
    "build:types": "tsc -p tsconfig.json",
    "dev": "concurrently 'pnpm:dev:*'",
    "dev:code": "pnpm build:code -- --watch",
    "dev:types": "nodemon --ext 'ts,tsx' --exec 'pnpm build:types'",
    "lint": "eslint --ext .ts,.tsx .",
    "test": "vitest run --config .viterc.ts",
    "test:coverage": "vitest run --coverage --config .viterc.ts",
    "test:dev": "vitest --config .viterc.ts",
    "version:prerelease": "npm version prerelease --preid=next --allow-same-version && git push --follow-tags"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.5.0",
    "@micra/commit-lint-config": "next",
    "@micra/core": "next",
    "@micra/eslint-config": "next",
    "@micra/typescript-config": "next",
    "@micra/vite-config": "next",
    "@types/node": "^18.15.7",
    "c8": "^7.11.0",
    "concurrently": "^7.0.0",
    "eslint": "^8.11.0",
    "eslint-plugin-prettier": "^4.0.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.0",
    "nodemon": "^2.0.22",
    "prettier": "^2.8.7",
    "typescript": "^5.0.2",
    "vite": "^4.2.1",
    "vitest": "^0.29.7"
  }
}
